// =========================
// src/pages/Conselho.jsx - Página do Conselho
import React from "react";
import { useUserStore } from "../store/userStore";

export default function Conselho() {
  const { nome } = useUserStore();

  return (
    <div className="min-h-screen bg-white text-black p-6 space-y-8">
      <h1 className="text-2xl font-bold">Bem-vindo(a), {nome}</h1>

      <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
        {['PEC', 'Inclusão Produtiva', 'Sócio Emocional', 'Negócios Sociais'].map((item) => (
          <button
            key={item}
            className="bg-yellow-400 text-black font-semibold p-4 rounded shadow hover:opacity-90"
          >
            {item}
          </button>
        ))}
      </div>

      <div className="mt-10">
        <h2 className="text-xl font-semibold mb-4">Gráfico Geral</h2>
        <div className="h-64 bg-gray-100 border rounded flex items-center justify-center text-gray-500">
          {/* Aqui será renderizado o gráfico geral com dados reais no futuro */}
          Gráfico será exibido aqui
        </div>
      </div>

      <div className="mt-10">
        <h2 className="text-xl font-semibold mb-4">Gráfico de Metas x Realizado</h2>
        <div className="h-64 bg-gray-100 border rounded flex items-center justify-center text-gray-500">
          {/* Aqui será renderizado o gráfico de colunas com metas */}
          Gráfico será exibido aqui
        </div>
      </div>
    </div>
  );
}

// =========================
// src/pages/Perfil.jsx - Página de Perfil (Login/Cadastro)
import React from "react";
import { useNavigate } from "react-router-dom";

export default function Perfil() {
  const navigate = useNavigate();

  return (
    <div className="min-h-screen bg-white text-black flex flex-col items-center justify-center p-6 space-y-6">
      <h1 className="text-3xl font-bold mb-6">Perfil</h1>

      <button
        onClick={() => navigate("/login")}
        className="w-full max-w-sm bg-black text-white py-3 rounded"
      >
        Entrar
      </button>

      <button
        onClick={() => navigate("/doar")}
        className="w-full max-w-sm border border-black text-black py-3 rounded"
      >
        Cadastre-se
      </button>
    </div>
  );
}
