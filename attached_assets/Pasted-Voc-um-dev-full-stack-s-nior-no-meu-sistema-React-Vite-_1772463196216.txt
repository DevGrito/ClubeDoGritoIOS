Você é um dev full-stack sênior no meu sistema (React/Vite/TS + Node/Express + Drizzle/Postgres).
Preciso implementar JUSTIFICATIVA DE FALTAS no fluxo da tela “Controle de Presença” (a mesma usada por coordenador, monitor e professor).

CONTEXTO ATUAL
- Existe a tela “Controle de Presença” com:
  - Select “Turma”
  - Select “Data da Aula”
  - Lista de alunos com status (ex.: “Presente” ou “Aguardando catraca”)
  - Botão verde “Finalizar Chamada”
- A presença via Intelbras/catraca entra automaticamente e marca o aluno como PRESENTE.
- Quem não passa na catraca fica como “Aguardando catraca” e hoje não temos como registrar/justificar falta antes de finalizar.

OBJETIVO
Ao clicar em “Finalizar Chamada” nessa mesma tela:
1) Identificar todos alunos vinculados à turma na data selecionada.
2) Quem NÃO estiver presente (ex.: status “Aguardando catraca”) deve ser tratado como AUSENTE.
3) Se houver ausentes sem justificativa, abrir um MODAL (pop-up) obrigatório nessa tela com:
   - Lista de alunos ausentes
   - Para cada aluno:
     - dropdown obrigatório “Motivo”: Doença, Escola, Trabalho, Transporte, Família, Outro, Sem justificativa
     - textarea opcional “Observação”
   - botão “Salvar justificativas e Finalizar”
   - opcional: “Aplicar motivo para todos”
4) Só finalizar a chamada quando todas ausências tiverem motivo (ou “Sem justificativa”).
5) Registrar auditoria: quem justificou e quando.

REGRAS IMPORTANTES
- Intelbras só marca PRESENÇA (status=presente, fonte=intelbras, checkin_at).
- AUSÊNCIA é gerada no FECHAMENTO do encontro (para quem não está presente).
- Monitor/Professor não pode finalizar com pendências. Coordenador/Admin pode (opcional), mas logar (se implementar).
- Deve funcionar tanto para chamada automática (catraca) quanto manual.

DADOS / MODELAGEM
Criar/usar conceito de ENCONTRO (sessão) por turma + data:
Tabela encontros:
- id, turma_id, data, status ('aberto'|'fechado'), inicio_real, fim_real, criado_por_user_id, fechado_por_user_id

Tabela encontro_participantes:
- id, encontro_id, aluno_id
- status ('presente'|'ausente')
- fonte_presenca ('intelbras'|'manual')
- checkin_at
- justificativa_motivo (nullable)
- justificativa_obs
- justificado_por_user_id
- justificado_em
Constraint unique(encontro_id, aluno_id)

BACKEND (Express)
Implementar endpoints:
- POST /api/encontros (ou /api/turmas/:turmaId/encontros) -> cria/retorna encontro para turma+data
- POST /api/encontros/:encontroId/checkin -> upsert presente (Intelbras/manual)
- POST /api/encontros/:encontroId/finalizar
  -> passo crítico:
     A) listar alunos vinculados à turma
     B) garantir linha em encontro_participantes para todos:
        - se não existe, criar ausente
     C) verificar pendências: ausentes com justificativa_motivo null
        - se existir => retornar 409 com lista pendencias (aluno_id, nome)
        - se não existir => fechar encontro (status=fechado, fim_real=now)
- PATCH /api/encontros/:encontroId/justificativas
  body: { itens: [{ alunoId, motivo, obs? }] }
  -> atualiza justificativas + auditoria

FRONTEND (React - tela “Controle de Presença”)
- No clique do botão “Finalizar Chamada”:
  - chamar POST /finalizar
  - se 200: sucesso + travar edição (ou atualizar status)
  - se 409: abrir modal com pendencias
- Modal (shadcn/ui Dialog):
  - render lista de pendencias
  - permitir preencher motivo/obs
  - botão “Salvar justificativas e Finalizar”:
     1) PATCH /justificativas
     2) POST /finalizar novamente
- Usar react-query mutations e invalidar queries da presença/encontro.

ENTREGÁVEIS
1) Migrações/Drizzle schema (encontros + encontro_participantes + enums)
2) Rotas Express (com zod, status 409 para pendências)
3) Modal na tela “Controle de Presença” integrado ao botão “Finalizar Chamada”
4) Garantir idempotência: não duplicar registros (unique + upsert)

Faça com mudanças mínimas no layout atual e mantendo o visual da tela.